#!/usr/bin/env fish
killall -r wl-clipboard-history
killall -r dunst
# /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
killall -r wlsunset

# ~/.config/hypr/swayidle.fish
killall -r ydotoold
killall -r nm-applet

killall -r swww

# Bar
killall -r waybar
# eww open bar
