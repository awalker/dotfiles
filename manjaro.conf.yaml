- defaults:
    link:
        relink: true
        create: true
    shell:
        stdout: true
        stderr: true

- shell:
  - echo Manjaro
  - pamac install --no-confirm albert xclip python-neovim neovim fish
  - chsh -s $(which fish)
  - source $HOME/.bashrc && fnm install 14