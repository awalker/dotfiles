- defaults:
    link:
        relink: true
        create: true
    shell:
        stdout: true
        stderr: true

- shell:
  - echo Manjaro
  - pamac install --no-confirm albert
  - source $HOME/.bashrc && fnm install 14